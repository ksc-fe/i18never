overwrite: true
schema: "http://localhost:3003"
generates:
  src/index.ts:
    documents: 'src/*.gql'
    plugins:
      - "typescript"
      # - "typescript-resolvers"
      - "typescript-operations"
      - "typescript-graphql-request"
